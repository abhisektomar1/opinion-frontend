/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['aceternity.com',"assets.aceternity.com","probo.in"],
    },
};

export default nextConfig;
